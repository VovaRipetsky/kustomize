apiVersion: apps/v1
kind: Deployment
metadata:
  name: sbdemo
spec:
  template:
    spec:
      containers:
        - name: sbdemo
          image: nginx:latest
          ports:
            - containerPort: 80
          env:
          - name: spring.profiles.active
            value: prod
